# Archive report: <PROVIDER> (<DATE>) [PRIVATE; publishes only under recorded provider opt-in]

**Plain summary.** <N> collections swept against <M> rules; headline
pass rates below. Every number carries a receipt; every rule carries its
source. Coverage is honest: structural checks plus pyQuARC <version>
under ruleset <sha12>, which auto-flags a portion of what a manual
review finds, not all of it.

| Rule | Class | Pass | Receipt |
|---|---|---|---|
| <rule-id> | MUST or SHOULD | <n>/<N> | [^r1] |

**What would help.** <At most three concrete offers: a generated fix
list, a rules_override for local checks, a pairing session. Offers,
not findings.>

**Boundaries.** Produced by Open Science Pillars, a community
open-source project; not a NASA, JPL, or PO.DAAC product. Rules under
dispute are excluded and routed upstream.

[^r1]: sweep run <id>, tool commit <sha>, receipt at <path>
