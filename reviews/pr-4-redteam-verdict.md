# Red-team verdict: PR 4 (badge path), round 1

VERDICT: BLOCK

Verified before the finding: all three selftests PASS under uv run with exit codes read directly, never piped; the A4/A5 hardening lands exactly as the PR 1 round 2 closest call asked (a record with no revision_id or an unreachable CMR fails closed as A5 with explicit unverifiable wording, file-based records are exempt as having nothing to bind, and the selftest covers mismatch, match, no-revision, unreachable, and file cases); the .gitignore belt makes the private tier structural (check-ignore confirms sweeps/ and __pycache__/ are held, and sweep.yml's git add -f is now required and correctly scoped to only the aggregate glob); the three-state DOI reporting prints registered, missing-reason declared, and malformed or absent counts in the aggregate so a conformant declaration can never again read as a bare miss, with the selftest asserting the state counts; and make_badge.py proves both refusals in its selftest, the R1 refusal with no opt-in on disk and the R6 refusal on a real failed attest, asserting no badge directory is ever created, with skip-env-checks structurally never accepted. The disclosure checks out forensically: 00e9290's make_badge declared no dependencies and carried a committed __pycache__ artifact, and d3581cb pins pyQuARC identically to quarc_attest, removes the artifact, gitignores the directory, and adds the make_badge selftest to ci.yml's gates step. The disclosure itself is the honesty norm working as designed.

Finding:

1. New register entry proposed (per the reviewer contract): verification-chain masking. Attack: a local gate run piped through tail, head, or grep swallows a failing exit status, so a broken gate commits and pushes as green; observed 2026-08-30 on commit 00e9290, where the make_badge selftest crashed on a missing dependency and the pipe to tail masked the crash. Consequence unmitigated: gates become decoration, and the observatory's receipts-on-everything property dies the first time a green turns out hollow. Designed-in mitigation, already present in this PR: every gate tool's selftest runs unmasked in ci.yml's gates step where a nonzero exit fails the job, and the standing rule is that a new gate tool lands its selftest in that step in the same PR that adds the tool; local verification reads exit codes, never output tails alone. Residual: local habits stay maskable, CI is the backstop, and prompt disclosure of a masked failure is the norm this incident set. The control exists; what blocks is that RED-TEAM.md gains no row for it in this PR, on the same day the register graduates toward org doctrine, and a rubric row is what makes the next reviewer walk this check instead of rediscovering it. Add the row (R11) and this PR is clean.

Non-blocking notes: the make_badge happy path (an actual emission) is untested by the selftest, acceptable since the refusals are the security property and the first real badge will exercise it under a full attest anyway; and the CI gates step now installs pyQuARC from GitHub on every run, a time cost worth caching eventually, never a correctness issue.

# Red-team verdict: PR 4 (badge path), round 2

VERDICT: APPROVE

The round 1 finding is verified fixed: commit 5c8ac69 adds the R11 row to RED-TEAM.md, and the row carries the full proposal, the attack (verification-chain masking, with the observed 2026-08-30 incident on commit 00e9290 named), the consequence (gates become decoration and the receipts-on-everything property dies the first time a green turns out hollow), the designed-in mitigation (every gate tool's selftest runs unmasked in ci.yml's gates step, a new gate tool lands its selftest there in the same PR, local verification reads exit codes rather than output tails), and the residual (local habits stay maskable, CI is the backstop, prompt disclosure is the norm the founding incident set). The register table now has eleven rows. The fix commit touches RED-TEAM.md only, so the round 1 verifications of the unchanged code carry forward: all three selftests PASS with unmasked exit codes, the exact R10 grep exits clean (RED-TEAM.md remains inside its disclosed exclusion, and the new row adds no pattern words regardless), the A4/A5 fail-closed hardening, the .gitignore structural belt with sweep.yml's scoped force-add, the three-state DOI reporting, and make_badge's two proven refusals all stand as verified in round 1.

Register IDs checked across both rounds: R1, R3, R4, R5, R6, R7, R9, R10, and the new R11.

Closest call: R11's local rule (read exit codes, never output tails alone) is a habit no tooling here can enforce, which the row's residual admits honestly; the CI gates step is the only structural backstop, and that is acceptable because it is exactly where a masked local failure gets caught before merge.
